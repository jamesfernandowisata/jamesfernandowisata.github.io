# Backgrounds

The desktop picks one image from `list.json` the first time the site opens in a
browser tab, then keeps that same image while you move between pages.

Replace `bg-contents.jpg` or `bg-wallpaper.jpg` to change the current options.
If you add a new image name, add its public path to `list.json`.
